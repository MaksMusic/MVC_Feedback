spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/reg_db?useSSL=false
    username: bestuser
    password: bestuser
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      platform: mysql
